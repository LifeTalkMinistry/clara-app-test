// Final budget activation motivation is owned by installBudgetSetupCopyCleanup.
// Keep this module intentionally side-effect free so the older DOM experiment
// cannot hide or replace the React-owned Step 5 header.
export {};
