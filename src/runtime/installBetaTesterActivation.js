/**
 * Retired legacy beta-code membership injector.
 *
 * Trial activation is now React-owned by ClaraTrialAccessGate at the CLARA Orb.
 * This module intentionally remains as an inert compatibility import so older
 * runtime registries do not install a second activation form or DOM observer.
 */
