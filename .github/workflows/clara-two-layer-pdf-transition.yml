name: CLARA two-layer PDF transition validation

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize
    paths:
      - .clara-two-layer-trigger

permissions:
  contents: write

jobs:
  build-validate-and-commit:
    runs-on: ubuntu-latest
    steps:
      - name: Check out pull-request branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
          show-progress: false

      - name: Restore surgical component
        id: patch
        continue-on-error: true
        shell: bash
        run: |
          test -f .clara-two-layer-trigger
          test -d .clara-two-layer-payload
          cat .clara-two-layer-payload/chunk-* | base64 --decode > /tmp/LearningMaterialModal.jsx
          test -s /tmp/LearningMaterialModal.jsx
          mv /tmp/LearningMaterialModal.jsx src/components/fresh/main-dashboard/learning-hub/modal/LearningMaterialModal.jsx

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build validation 1
        run: npm run build

      - name: Build validation 2
        run: npm run build

      - name: Confirm surgical final tree
        shell: bash
        run: |
          rm -rf .clara-two-layer-payload .clara-two-layer-trigger
          git diff --check
          test -z "$(git diff --name-only -- package.json package-lock.json)"
          git fetch origin main --quiet
          changed="$(git diff --name-only origin/main -- src/components/fresh/main-dashboard/learning-hub/modal/LearningMaterialModal.jsx .clara-two-layer-payload .clara-two-layer-trigger)"
          test "$changed" = "src/components/fresh/main-dashboard/learning-hub/modal/LearningMaterialModal.jsx"
          unexpected="$(git status --short | sed 's/^...//' | grep -Ev '^(src/components/fresh/main-dashboard/learning-hub/modal/LearningMaterialModal.jsx|.clara-two-layer-payload/.*|.clara-two-layer-trigger)$' || true)"
          test -z "$unexpected"

      - name: Commit validated implementation
        shell: bash
        run: |
          git config user.name "CLARA Build Bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A -- src/components/fresh/main-dashboard/learning-hub/modal/LearningMaterialModal.jsx .clara-two-layer-payload .clara-two-layer-trigger
          git commit -m "Fix PDF transition with two rendered layers [skip ci]"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
