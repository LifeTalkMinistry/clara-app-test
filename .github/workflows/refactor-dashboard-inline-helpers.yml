name: Refactor dashboard inline helpers

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Dashboard refactor workflow disabled
        run: |
          echo "Dashboard refactor automation is disabled after stable recovery."
          echo "Do not run extraction scripts against Dashboard.jsx without a fresh manual review."
