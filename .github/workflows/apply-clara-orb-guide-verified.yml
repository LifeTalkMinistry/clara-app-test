name: Verify CLARA Orb Guide Refactor

# Trigger the default-branch verifier after diagnostic capture was enabled.
on:
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  placeholder:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "The active verifier is loaded from main."
